import { style } from "@vanilla-extract/css";

export const executeButtonContainerStyle = {
  container: style({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    margin: "4rem 0",
  }),
};
